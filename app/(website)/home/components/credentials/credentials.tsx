import Credential from "./credential";

const Credentials = () => {
  return (
    <div className="mt-12 border-t pt-12 dark:border-[#202021] duration-200">
      <h2 className="text-medium font-semibold">Credentials</h2>
      <div className="mt-8">
        <Credential />
      </div>
    </div>
  );
};

export default Credentials;
